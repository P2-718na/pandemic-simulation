ADD_EXECUTABLE(seed main.cpp)

ADD_SUBDIRECTORY(Seeder)

TARGET_INCLUDE_DIRECTORIES(seed PUBLIC Seeder)

TARGET_LINK_LIBRARIES(seed Seeder)

SET_TARGET_PROPERTIES(seed PROPERTIES
        RUNTIME_OUTPUT_DIRECTORY "${PROJECT_BINARY_DIR}")
