ADD_EXECUTABLE(lab main.cpp)

INCLUDE_DIRECTORIES(
        lab PUBLIC
        "${PROJECT_SOURCE_DIR}/src"
        "${PROJECT_SOURCE_DIR}/src/Bitmap"
)

ADD_SUBDIRECTORY(Bitmap)

TARGET_LINK_LIBRARIES(lab Bitmap)